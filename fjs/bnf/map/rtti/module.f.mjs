/**
 * @import { Rule } from '../../types.ts'
 * @import { RuleInfo, Base } from './types.ts'
 */

import { todo } from '../../../asserts/module.f.mjs'

/** @type {(ri: readonly RuleInfo[]) => Map<Rule, Base>} */
const checkMap = ri => todo()
