# Changelog

The changelog is the [`changelog/`](./changelog/) directory: one file per
released version, plus `changelog/unreleased/` holding one file per pull request
that is not released yet. See [changelog/README.md](./changelog/README.md).
