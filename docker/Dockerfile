# Generated by `fjs ci` from fjs/ci/docker/module.f.ts. Do not edit by hand.
#
# Every version below is pinned in fjs/ci/config/module.f.ts, the same source
# .github/workflows/ci.yml is generated from. Change a pin there and rerun
# `npm run ci-update`; CI fails when the committed files no longer match.

# Date-stamped snapshot of the distribution the `ubuntu-26.04` runners use.
FROM ubuntu:resolute-20260707
SHELL ["/bin/bash", "-euo", "pipefail", "-c"]

# Build prerequisites. Their versions come from the pinned base-image snapshot.
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential ca-certificates curl git gnupg libssl-dev pkg-config unzip xz-utils \
    && rm -rf /var/lib/apt/lists/*

# Node.js: official build, verified against the release SHASUMS256.txt.
ENV NODE_VERSION=26.5.0
RUN arch="$(dpkg --print-architecture)" \
    && case "$arch" in amd64) node_arch=x64 ;; arm64) node_arch=arm64 ;; *) echo "unsupported architecture: $arch" >&2; exit 1 ;; esac \
    && node_url="https://nodejs.org/dist/v$NODE_VERSION" \
    && node_archive="node-v$NODE_VERSION-linux-$node_arch.tar.xz" \
    && curl -fsSLo "/tmp/$node_archive" "$node_url/$node_archive" \
    && curl -fsSL "$node_url/SHASUMS256.txt" | grep " $node_archive$" | (cd /tmp && sha256sum -c -) \
    && tar -xJf "/tmp/$node_archive" -C /usr/local --strip-components=1 --no-same-owner \
    && rm "/tmp/$node_archive"

# Deno: official release archive.
ENV DENO_VERSION=2.9.4
RUN arch="$(dpkg --print-architecture)" \
    && case "$arch" in amd64) deno_arch=x86_64-unknown-linux-gnu ;; arm64) deno_arch=aarch64-unknown-linux-gnu ;; *) echo "unsupported architecture: $arch" >&2; exit 1 ;; esac \
    && curl -fsSLo /tmp/deno.zip "https://github.com/denoland/deno/releases/download/v$DENO_VERSION/deno-$deno_arch.zip" \
    && unzip -q -d /usr/local/bin /tmp/deno.zip \
    && chmod +x /usr/local/bin/deno \
    && rm /tmp/deno.zip

# Bun: official release archive.
ENV BUN_VERSION=1.3.14
RUN arch="$(dpkg --print-architecture)" \
    && case "$arch" in amd64) bun_arch=x64 ;; arm64) bun_arch=aarch64 ;; *) echo "unsupported architecture: $arch" >&2; exit 1 ;; esac \
    && curl -fsSLo /tmp/bun.zip "https://github.com/oven-sh/bun/releases/download/bun-v$BUN_VERSION/bun-linux-$bun_arch.zip" \
    && unzip -q -d /tmp /tmp/bun.zip \
    && mv "/tmp/bun-linux-$bun_arch/bun" /usr/local/bin/bun \
    && chmod +x /usr/local/bin/bun \
    && rm -rf /tmp/bun.zip "/tmp/bun-linux-$bun_arch"

# Rust: pinned rustup installs the pinned toolchain, its components and targets.
ENV RUSTUP_VERSION=1.29.0
ENV RUST_VERSION=1.97.1
ENV RUSTUP_HOME=/usr/local/rustup
ENV CARGO_HOME=/usr/local/cargo
ENV PATH=/usr/local/cargo/bin:$PATH
RUN arch="$(dpkg --print-architecture)" \
    && case "$arch" in amd64) rust_host=x86_64-unknown-linux-gnu ;; arm64) rust_host=aarch64-unknown-linux-gnu ;; *) echo "unsupported architecture: $arch" >&2; exit 1 ;; esac \
    && curl -fsSLo /tmp/rustup-init "https://static.rust-lang.org/rustup/archive/$RUSTUP_VERSION/$rust_host/rustup-init" \
    && chmod +x /tmp/rustup-init \
    && /tmp/rustup-init -y --no-modify-path --profile minimal --default-toolchain $RUST_VERSION --component rustfmt clippy --target wasm32-wasip1 wasm32-wasip2 wasm32-unknown-unknown wasm32-wasip1-threads \
    && rm /tmp/rustup-init \
    && chmod -R a+w $RUSTUP_HOME $CARGO_HOME

# Wasmtime: runner for the WASM targets.
ENV WASMTIME_VERSION=47.0.2
RUN arch="$(dpkg --print-architecture)" \
    && case "$arch" in amd64) wasmtime_arch=x86_64 ;; arm64) wasmtime_arch=aarch64 ;; *) echo "unsupported architecture: $arch" >&2; exit 1 ;; esac \
    && wasmtime_dir="wasmtime-v$WASMTIME_VERSION-$wasmtime_arch-linux" \
    && curl -fsSLo /tmp/wasmtime.tar.xz "https://github.com/bytecodealliance/wasmtime/releases/download/v$WASMTIME_VERSION/$wasmtime_dir.tar.xz" \
    && tar -xJf /tmp/wasmtime.tar.xz -C /tmp \
    && mv "/tmp/$wasmtime_dir/wasmtime" /usr/local/bin/wasmtime \
    && rm -rf /tmp/wasmtime.tar.xz "/tmp/$wasmtime_dir"

# Wasmer: runner used by .cargo/config.wasmer.toml, including the threads target.
ENV WASMER_VERSION=7.2.1
ENV WASMER_DIR=/usr/local
RUN arch="$(dpkg --print-architecture)" \
    && case "$arch" in amd64) wasmer_arch=amd64 ;; arm64) wasmer_arch=aarch64 ;; *) echo "unsupported architecture: $arch" >&2; exit 1 ;; esac \
    && curl -fsSLo /tmp/wasmer.tar.gz "https://github.com/wasmerio/wasmer/releases/download/v$WASMER_VERSION/wasmer-linux-$wasmer_arch.tar.gz" \
    && tar -xzf /tmp/wasmer.tar.gz -C /usr/local \
    && rm /tmp/wasmer.tar.gz

# Playwright and its browsers. Browser builds are pinned by the Playwright version.
ENV PLAYWRIGHT_VERSION=1.62.0
ENV PLAYWRIGHT_BROWSERS_PATH=/usr/local/ms-playwright
RUN npm install -g playwright@$PLAYWRIGHT_VERSION \
    && playwright install --with-deps chromium firefox webkit \
    && rm -rf /var/lib/apt/lists/*

# The published FunctionalScript CLI (`fjs`) used by the generated smoke tests.
ENV FUNCTIONALSCRIPT_VERSION=0.38.0
RUN npm install -g functionalscript@$FUNCTIONALSCRIPT_VERSION

# The repository is mounted or checked out here; the image itself carries only tools.
WORKDIR /workspace
CMD ["bash"]
