export default [
    {"scope": ["set", "reject"], "role": "serializer", "because": "a reject class names a document a reader refuses, and a serializer refuses no input at all; it is handed a value of the data model and its type is the contract"},
    {"scope": ["subtree", "id"], "role": "serializer", "because": "a const name is the serializer's to choose, so the shape of one is the reader's branch; normalized form is where naming is pinned"},
    {"scope": ["subtree", "ws"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "whitespace"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "document"], "role": "serializer", "because": "the document's own edges and spellings are what a reader takes, and a serializer may emit any of them; normalized form is where one is chosen"},
    {"scope": ["subtree", "number/exp"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/zero"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/frac/one-digit"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/frac/high-first"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/frac-exp"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/int/digit9"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/int/digits"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/binary64/rounding"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "number/binary64/overflow"], "role": "serializer", "because": "the value is an infinity, which the serializer set carries as a leaf of its own; this class is the reader's spelling of one"},
    {"scope": ["subtree", "number/binary64/underflow"], "role": "serializer", "because": "the value is a zero, which the serializer set carries with both signs; this class is the reader's spelling of one"},
    {"scope": ["subtree", "bigint/digit9"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "bigint/zero/neg"], "role": "serializer", "because": "the value is zero, which a bigint has only one of; the serializer set carries it, and the negative spelling is the reader's"},
    {"scope": ["subtree", "string/escape/u"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "key/string/escape/u"], "role": "serializer", "because": "a serializer is handed a graph and picks its own spelling, so this is the reader's branch; what a normalized serializer must emit is pinned under that role"},
    {"scope": ["subtree", "string/escape/slash"], "role": "serializer", "because": "the value is the same one the serializer set carries; only the document spelling differs"},
    {"scope": ["subtree", "key/string/escape/slash"], "role": "serializer", "because": "the value is the same one the serializer set carries; only the document spelling differs"},
    {"scope": ["subtree", "object/duplicate"], "role": "serializer", "because": "a duplicate key is a document fact and never a graph fact, so no input a serializer is handed can carry one"},
    {"scope": ["subtree", "object/key-order/non-index"], "role": "serializer", "because": "observable key order is one property and the serializer set pins it once, with an object mixing index and string keys; the rest are the reader's readings of a document"},
    {"scope": ["subtree", "object/key-order/boundaries"], "role": "serializer", "because": "observable key order is one property and the serializer set pins it once, with an object mixing index and string keys; the rest are the reader's readings of a document"},
    {"scope": ["subtree", "object/key-order/index-first"], "role": "serializer", "because": "observable key order is one property and the serializer set pins it once, with an object mixing index and string keys; the rest are the reader's readings of a document"},
    {"scope": ["subtree", "object/key-order/escaped-index"], "role": "serializer", "because": "observable key order is one property and the serializer set pins it once, with an object mixing index and string keys; the rest are the reader's readings of a document"},
    {"scope": ["subtree", "const/reference"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/one"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/two"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/value"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/unreferenced"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/unshared"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/shared/leaf"], "role": "serializer", "because": "a shared leaf is invisible in a graph, since leaves compare by value and not by identity, so no input can carry one"},
    {"scope": ["subtree", "const/shared/nested"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "const/shared/three-paths"], "role": "serializer", "because": "a const is how a document spells sharing, not a fact about the graph, so a serializer chooses whether and how to bind; what it may not choose is hoisting a node reached twice, which the sharing vectors carry"},
    {"scope": ["subtree", "array/two"], "role": "serializer", "because": "the serializer set covers containers by shape rather than by element count, since a walker cannot tell two from three without telling one from two"},
    {"scope": ["subtree", "object/two"], "role": "serializer", "because": "the serializer set covers containers by shape rather than by element count, since a walker cannot tell two from three without telling one from two"},
    {"scope": ["subtree", "array/object"], "role": "serializer", "because": "the serializer set covers containers by shape rather than by element count, since a walker cannot tell two from three without telling one from two"},
    {"scope": ["subtree", "array/nested/deep"], "role": "serializer", "because": "the specification states no depth an implementation must support, so no vector can say which depth conforming means; a writer's own recursion limit is real and tracked where it belongs, in fjs/media/datajs/todo/serializer.md, and a data module cannot spell a graph deep enough to find one"},
    {"scope": ["subtree", "object/nested/deep"], "role": "serializer", "because": "the specification states no depth an implementation must support, so no vector can say which depth conforming means; a writer's own recursion limit is real and tracked where it belongs, in fjs/media/datajs/todo/serializer.md, and a data module cannot spell a graph deep enough to find one"},
    {"scope": ["subtree", "array/nested/empty"], "role": "serializer", "because": "the serializer set covers containers by shape rather than by element count, since a walker cannot tell two from three without telling one from two"},
    {"scope": ["subtree", "object/nested/empty"], "role": "serializer", "because": "the serializer set covers containers by shape rather than by element count, since a walker cannot tell two from three without telling one from two"},
    {"scope": ["subtree", "array/elements/negative-first"], "role": "serializer", "because": "the serializer set covers containers by shape rather than by element count, since a walker cannot tell two from three without telling one from two"},
    {"scope": ["subtree", "string/surrogate/lone/raw"], "role": "serializer", "because": "a lone surrogate is one value however a document spells it, and the serializer set carries all four"},
    {"scope": ["subtree", "key/string/surrogate/lone/raw"], "role": "serializer", "because": "a lone surrogate is one value however a document spells it, and the serializer set carries all four"},
    {"scope": ["subtree", "string/surrogate/pair/mixed"], "role": "serializer", "because": "the serializer set carries a pair, and what it must not do is assert a spelling for one; the corners are the reader's readings of four escapes"},
    {"scope": ["subtree", "string/surrogate/pair/low-corner"], "role": "serializer", "because": "the serializer set carries a pair, and what it must not do is assert a spelling for one; the corners are the reader's readings of four escapes"},
    {"scope": ["subtree", "string/surrogate/pair/high-corner"], "role": "serializer", "because": "the serializer set carries a pair, and what it must not do is assert a spelling for one; the corners are the reader's readings of four escapes"},
    {"scope": ["subtree", "key/string/surrogate/pair/mixed"], "role": "serializer", "because": "the serializer set carries a pair, and what it must not do is assert a spelling for one; the corners are the reader's readings of four escapes"},
    {"scope": ["subtree", "key/string/surrogate/pair/low-corner"], "role": "serializer", "because": "the serializer set carries a pair, and what it must not do is assert a spelling for one; the corners are the reader's readings of four escapes"},
    {"scope": ["subtree", "key/string/surrogate/pair/high-corner"], "role": "serializer", "because": "the serializer set carries a pair, and what it must not do is assert a spelling for one; the corners are the reader's readings of four escapes"},
    {"scope": ["subtree", "string/raw/range/0020-0021/high"], "role": "serializer", "because": "the serializer set carries the low end of this range and ordinary content above it"},
    {"scope": ["subtree", "key/string/raw/range/0020-0021/high"], "role": "serializer", "because": "the serializer set carries the low end of this range and ordinary content above it"},
    {"scope": ["subtree", "string/raw/range/005d-10ffff/low"], "role": "serializer", "because": "the serializer set carries the high end of this range and ordinary content within it"},
    {"scope": ["subtree", "key/string/raw/range/005d-10ffff/low"], "role": "serializer", "because": "the serializer set carries the high end of this range and ordinary content within it"},
    {"scope": ["subtree", "key/proto/value"], "role": "serializer", "because": "the serializer set carries an object with an own enumerable proto member; what that member holds is the ordinary value coverage above it"},
    {"scope": ["set", "normalize"], "role": "reader", "because": "normalized form is one serializer's output rule, so a class only that set carries names a spelling and not a document a reader must take"},
    {"scope": ["set", "normalize"], "role": "serializer", "because": "an ordinary serializer owes a valid document denoting the input and never a spelling, so a class only the normalize set carries is outside what this role is judged on"},
    {"scope": ["set", "reject"], "role": "normalize", "because": "a reject class names a document a reader refuses, and normalized form emits only documents, so it has no output for one"},
    {"scope": ["subtree", "id"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "ws"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "number/exp"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "object/duplicate"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "string/escape/u"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "key/string/escape/u"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "leaf"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "number/zero"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "array/nested"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "object/nested"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "const/reference"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "document/trailing"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "number/frac/one-digit"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "string/surrogate/pair"], "role": "normalize", "because": "the four lone surrogates and the pair are pinned, and the escaping rule is per code unit, so an adjacency's spelling follows from theirs with nothing left to decide"},
    {"scope": ["subtree", "string/surrogate/lone/raw"], "role": "normalize", "because": "the four lone surrogates and the pair are pinned, and the escaping rule is per code unit, so an adjacency's spelling follows from theirs with nothing left to decide"},
    {"scope": ["subtree", "key/string/surrogate/pair"], "role": "normalize", "because": "the four lone surrogates and the pair are pinned, and the escaping rule is per code unit, so an adjacency's spelling follows from theirs with nothing left to decide"},
    {"scope": ["subtree", "key/string/surrogate/lone/raw"], "role": "normalize", "because": "the four lone surrogates and the pair are pinned, and the escaping rule is per code unit, so an adjacency's spelling follows from theirs with nothing left to decide"},
    {"scope": ["subtree", "key/proto/value"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "bigint/digit9"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["class", "array/elements/negative-first"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "number/frac-exp"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["class", "object/unshared"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "document/consts"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "document/leading"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "number/int/digit9"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "const/unreferenced"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "number/frac/high-first"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "number/binary64/rounding"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "number/binary64/overflow"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "number/binary64/underflow"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "array/one"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "array/two"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "const/one"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "const/two"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "object/one"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "object/two"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "array/empty"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "array/three"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "const/value"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "object/empty"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "object/three"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "array/object"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "const/unshared"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "bigint/zero/neg"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "document/shortest"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "string/escape/slash"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "const/shared/nested"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "document/both-edges"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "key/string/escape/slash"], "role": "normalize", "because": "normalized form emits one spelling per value, and this class is another spelling the reader takes for a value the set already pins"},
    {"scope": ["subtree", "const/shared/three-paths"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "document/spelling/readable"], "role": "normalize", "because": "this is a fact about a document a reader takes, and normalized form is handed a graph, so the class has no normalized output of its own"},
    {"scope": ["subtree", "object/key-order/index-first"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "object/key-order/escaped-index"], "role": "normalize", "because": "the bytes are pinned by the vector that carries this value under its own class; the shape here varies only in a count or a depth, which normalized layout does not branch on"},
    {"scope": ["subtree", "byte"], "role": "serializer", "because": "a byte document is a reader's input; a serializer is handed a graph and emits a document as text, so encoding it is the caller's"},
    {"scope": ["subtree", "byte"], "role": "normalize", "because": "a byte document is a reader's input; normalized form is handed a graph and produces text, so encoding it is the caller's"}
];
