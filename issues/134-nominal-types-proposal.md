# 134. A proposal for nominal types in TypeScript.

**Priority:** P3
**Status:** open

A proposal for nominal types in TypeScript. The main reason is that the current `Nominal` type doesn't support type narrowing properly.
