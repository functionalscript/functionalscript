# 24. Create `./fsc.ts`

**Priority:** P5
**Status:** open

Create `./fsc.ts` that supports the same behavior as the current NaNVM Rust implementation:

- [ ] run `node ./fsc.ts input.f.js output.f.js`
- [ ] run `deno ./fsc.ts input.f.js output.f.js`
