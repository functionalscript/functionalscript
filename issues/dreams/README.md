# Dreams

- UTF8 strings instead of UTF16
- integer number `123` is a `bigint`.
- assigning `undefined` to a property
  ```ts
  const x = { a: undefined } // x is {}
  ```
- pipeline operator
- automatic binding
  ```ts
  const m = [42].at
  m(0) // 42
  ```
