/**
 * Compiled by `fjs compile` into the sibling `string.rs`, committed and
 * drift-checked by `npm run gen` (see `../../fjs/ci/README.md`). Consumed by
 * `../src/lib.rs`.
 */
export default "hello";
