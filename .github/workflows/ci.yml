{
  "on": {
    "pull_request": {}
  },
  "jobs": {
    "ubuntu": {
      "runs-on": "ubuntu",
      "steps": [
        {
          "run": "npm test"
        }
      ]
    },
    "macos": {
      "runs-on": "macos",
      "steps": [
        {
          "run": "npm test"
        }
      ]
    },
    "windows": {
      "runs-on": "windows",
      "steps": [
        {
          "run": "npm test"
        }
      ]
    }
  }
}