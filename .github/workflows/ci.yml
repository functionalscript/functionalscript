{
  "on": {
    "pull_request": {}
  },
  "jobs": {
    "ubuntu": {
      "runs-on": "ubuntu-latest",
      "steps": [
        {
          "run": "npm test"
        }
      ]
    },
    "macos": {
      "runs-on": "macos-latest",
      "steps": [
        {
          "run": "npm test"
        }
      ]
    },
    "windows": {
      "runs-on": "windows-latest",
      "steps": [
        {
          "run": "npm test"
        }
      ]
    }
  }
}