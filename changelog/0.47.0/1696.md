- `js/string_escape`: new module — the simple string escapes as one table, with
  encode and decode views derived from it. The JSON serializer and both
  tokenizers now read it instead of each keeping a copy; output is unchanged and
  no longer disturbed by a numeric property on `Object.prototype`
