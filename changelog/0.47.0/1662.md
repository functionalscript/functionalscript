- **BREAKING CHANGES:** `edag`: `UndefinedOp`, `Args`, and `Frame` (plus their
  `undefinedOp`/`args`/`frame` runtime exports) are replaced by a third generic,
  id-parameterized node kind — `Op0`/`op0Id`/`op0` (`undefined`/`args`/`frame`).
  `UnaryOp`/`UnaryOpId`/`unaryOp`/`unaryOpId` and `BinaryOp`/`BinaryOpId`/
  `binaryOp`/`binaryOpId` (added in the previous PR) are renamed to
  `Op1`/`Op1Id`/`op1`/`op1Id` and `Op2`/`Op2Id`/`op2`/`op2Id`, joining `Op0` under
  one naming scheme
