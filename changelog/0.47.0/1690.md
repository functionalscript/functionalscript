- `types/bigfloat`: new `tryDecToFormat` and the `binary64` format — a decimal
  rounds **once** onto the target's grid, so subnormals and overflow come out
  correctly-rounded. `decToBin` keeps its unbounded 53-bit contract unchanged
