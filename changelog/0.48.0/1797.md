- `djs`: an unterminated string or comment, and unmatched input, report the
  span of the offending source as `path:line:column-column` rather than only
  its start. A malformed number still points at the character that spoiled it.
