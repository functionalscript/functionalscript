- **BREAKING CHANGES:** `fjs/fsc`: the unused, unproven `bnf.f.mjs` and
  `json.f.mjs` BNF grammars are removed, with no drop-in replacement.
  `deterministic` in `fjs/bnf/testlib.f.mjs` is the surviving JSON grammar, but
  it wraps the value rule in whitespace and replaces none of the other exports.
