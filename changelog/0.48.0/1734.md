- **BREAKING CHANGES:** `website`/`emergent_testing/browser`: the generated
  browser test page starts idle instead of auto-running on load, and drops the
  `run` query parameter; its control is renamed `Run` and stays genuinely
  disabled while a suite is loading or running
