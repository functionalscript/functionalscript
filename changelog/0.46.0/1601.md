- **BREAKING CHANGES:** `cas`: `casUpload` is removed. It had no caller and
  was the remnant of an upload flow deleted in 0.32.2; content is added through
  `casAddFile` (CLI) or the MCP `cas_add` tool
