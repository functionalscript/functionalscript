- `nanvm-lib`: remove the `Serializable` trait, its per-type impls and tag
  constants, the little-endian helpers, and the `IContainer` serialization
  defaults; the tagged binary format is superseded by CBOR of `Any`
- the language spec drops the `Tag` columns from its JSON, DJS, and FJS tables
