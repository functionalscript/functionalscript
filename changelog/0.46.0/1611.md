- **BREAKING CHANGES:** `bnf/ll1`: the AST now matches `bnf/descent`'s — a node
  per rule invocation, entered before its first symbol is consumed, and a flat
  node per `Repeat` — so the AST is one contract across backends. `dispatchMap`
  stores per-rule first sets instead of consumed-symbol rule chains and throws
  `left recursion …` at build time for a left-recursive grammar
