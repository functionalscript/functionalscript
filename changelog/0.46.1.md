- `types/rtti`: new `validate/module.f.mjs`. `validate(schema)(value)` accepts
  what `parse` accepts but returns the original value rather than a
  reconstruction — undeclared members survive, absent optional members stay
  absent, and the result is reference-identical to the input. (#1645)
