- **BREAKING CHANGES:** `emergent_testing`: `Reporter.summary` receives a
  `RunState` — the totals plus the run's failures in order — where it took
  `RunTotals`
- `fjs t`: a failing test writes only its pass/fail line as it runs; the value
  it failed with, or its GitHub `::error` annotation, is written with the other
  failures once the run has ended
- **BREAKING CHANGES:** `emergent_testing`: `Reporter` gained `start`, called
  with a `TestId` before a leaf runs; `TestResult` is now that identity plus
  the outcome
- `fjs t`: each test is named before it runs — `name: running` — so a slow test
  is visible while it runs and a run that dies names the test it died in
