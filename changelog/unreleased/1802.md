- `emergent_testing`: new `LeafReporter` — the three events a leaf produces
  (`start`, `result`, `test`) — split out of `Reporter`, which is now that plus
  `summary`. `runEntries` takes the smaller one, so a host that ends its own
  run supplies no `summary`
- `emergent_testing`: `runEntries` has no error channel. Every failure a leaf's
  chain meets is answered as `RunState.aborted`, so a caller reads it there
  instead of handling a channel that could not fire
