- `protocol/mcp`: **breaking** — `McpConfig.protocolVersion` (one string) is
  replaced by `protocolVersions`, a non-empty latest-first list. `initialize`
  now echoes the client's requested revision when the server supports it and
  counter-proposes the latest supported one otherwise.
