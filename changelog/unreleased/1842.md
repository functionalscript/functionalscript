- `website`: the browser proof manifest generator refuses a module read that
  failed for anything but a missing path, rather than reading an oversized
  module as importing nothing
- `effects/node`: `readFile`'s size-cap failure names the file it refused, on
  the Node runner and the virtual one alike — a caller that stops on it could
  otherwise only report a build broken by no file in particular
