- **BREAKING CHANGES:** `fjs/types/rtti/` moved to `fjs/rtti/` — it is a peer
  of `djs`, not a member of `types/`. No code changed, only import paths;
  every importer inside and outside the repo needs the `types/` segment
  dropped from `functionalscript/fjs/types/rtti/…`.
