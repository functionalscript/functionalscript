- **BREAKING CHANGES:** `types/rtti`, `types/rtti/validate`, `types/rtti/parse`,
  `types/result` and `bnf` take `const` type parameters, so a literal argument
  keeps its literal type with no cast at the call site — and `or` infers a
  `readonly` tuple, so a schema built with it is `Or<readonly [A, B]>`.
