- **BREAKING CHANGES:** `types/rtti`, `types/rtti/validate`, `types/rtti/parse`,
  `types/result`, `protocol/mcp`'s `toolEntry` and `bnf`'s `option` take `const`
  type parameters, so a literal argument keeps its literal type with no cast at
  the call site — and `or` infers a `readonly` tuple, so a schema built with it
  is `Or<readonly [A, B]>`.
