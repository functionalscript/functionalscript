- `emergent_testing`: a proof whose return value cannot be enumerated — an
  enumerable getter or a proxy trap that throws — is now reported as that
  proof's failure instead of taking the whole run down with it. `effects`: adds
  the `catch` operation this needs, beside `sandbox`
