- **BREAKING CHANGES:** `website`: `website/browser-prepare.mjs` is removed. The
  browser proof manifest is generated by `website/module.f.mjs`'s program, so
  the `website` npm script is
  `node ./fjs/module.mjs r ./fjs/website/module.f.mjs`
- `effects/node`: a `Dirent` carries `isDirectory`. It is not `!isFile` — a
  symbolic link is neither — so a walk can ask what it actually needs before
  descending
