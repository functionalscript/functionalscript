- `media/type`: `detect` reads the magic state off the streaming detector's
  fold instead of driving a second copy of it; same results, but it now reads a
  whole text `Vec` rather than stopping at the first non-signature byte
