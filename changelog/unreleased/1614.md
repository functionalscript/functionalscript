- **BREAKING CHANGES:** `text/utf16`: the decoder's out-of-range guard emits the
  shared `errorMask` instead of a magic `0xffffffff`, matching the UTF-8 decoder.
  A code unit outside `[0x0000, 0xFFFF]` or non-integer now decodes to
  `2147483648`, not `4294967295`.
