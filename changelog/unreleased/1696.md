- `js/string_escape`: new module — the simple string escapes as one table, with
  the encode and decode views derived from it. `media/json`'s serializer and
  both tokenizers now read it instead of keeping a copy each; no behavior change
