- `website`/`emergent_testing/browser`: the generated browser test page starts
  idle instead of auto-running on load, drops the `run` query parameter, and
  labels its control `Run` instead of `Run again`; the control is now a
  genuinely disabled control (not a click-ignoring one) while a suite is
  loading or running, and reactivates on every terminal state
