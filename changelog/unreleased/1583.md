- `mcp/cas`: `cas_get`'s unreachable `fromVec`/`base64Encode` null-checks
  become `assertNotNullish`; adds proof coverage for `cas_add`'s
  `writeBytes`-failure race and `cas_get`'s hash-vanished-between-reads
  races, bringing the module to 100% line/branch/function coverage
