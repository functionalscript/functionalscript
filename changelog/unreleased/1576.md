- `cas`: adds proof coverage for `write`'s op-failure branches (a
  `writeBytes`/lease-renewal-`rename` call failing, and a mismatched final
  `stat`), bringing the module to 100% line/branch/function coverage
