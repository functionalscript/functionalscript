- Generated `private.d.ts` files are no longer published: `package.json`'s
  `files` excludes them, dropping 16 files from the package. They were
  implementation-private by contract — `types.ts` may not depend on
  `private.ts`, so no public declaration needed one — and the package's own
  declarations are type-checked in CI as an outside consumer installs them.
