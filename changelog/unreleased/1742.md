- **BREAKING CHANGES:** `emergent_testing`: the browser runner awaits only
  `instanceof Promise` values, as `fjs t` does. A promise from another realm is
  walked as a proof tree rather than awaited; authored FunctionalScript cannot
  produce one, and the browser suite runs authored FunctionalScript only
