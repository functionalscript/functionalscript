- **BREAKING CHANGES:** `emergent_testing`: the browser runner awaits only
  `instanceof Promise` values, as `fjs t` does. A promise from another realm, or
  a value impersonating one, is walked as a proof tree rather than awaited — the
  `Symbol.species` machinery that handled those is gone
