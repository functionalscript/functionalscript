- **BREAKING CHANGES:** `fjs/fsc`: the unused `bnf.f.mjs` and `json.f.mjs` BNF
  grammars are removed. They had no importer, no proof coverage, and the JSON
  half duplicated `deterministic` in `fjs/bnf/testlib.f.mjs`; an importer of
  `functionalscript/fjs/fsc/json.f.mjs` should use that instead.
