- `emergent_testing`: the browser report's results carry a `name` — the test
  identity `fjs t` prints, built by the same `fmtImport` function — and the page
  renders it, so both runners spell a test identically
