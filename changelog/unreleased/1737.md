- **BREAKING CHANGES:** `emergent_testing`: `browser.mjs` moves to
  `browser/module.mjs` and now shares `fjs t`'s proof semantics;
  `runBrowserProofs` and `startBrowserTests` are gone, `startBrowserTestSources`
  remains. Only `instanceof Promise` values are awaited, matching `fjs t`
- `effects`: the host-independent operations (`all`, `await`, `fetch`, `import`,
  `now`, `sandbox`) move to `effects/common`, re-exported unchanged from
  `effects/node`; `effects/browser` interprets them in a browser realm
