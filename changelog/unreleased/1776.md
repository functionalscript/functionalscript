- **BREAKING CHANGES:** `nanvm`: the shared operator corpus reuses the
  canonical EDAG operation ids — a group carries an `Op1Id` or an `Op2Id`, and
  its cases the operand count that id fixes, replacing `Op`. `rust`'s `call`
  gives way to `nodeExpr`, printing the EDAG expression each case now denotes.
