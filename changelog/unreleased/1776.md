- **BREAKING CHANGES:** `nanvm`: the shared operator corpus reuses the
  canonical EDAG operation ids — a group carries an `Op1Id` or an `Op2Id` with
  `Case<N>` operands to match, replacing `Op`, and `rust`'s `call` gives way to
  `nodeExpr`, which prints the EDAG expression each case now denotes.
