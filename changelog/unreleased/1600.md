- `djs/tokenizer`: restores 100% branch coverage — narrows
  `stringDecodeScan`'s escape-character switch via `assert` instead of a
  dead default arm, and removes an unreachable fallback in `tokenizeJs`'s
  error-position lookup
