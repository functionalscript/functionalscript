- `emergent_testing`: `proof.f.mjs` no longer constructs promises — the last
  `Promise` construction in any authored `.f.mjs`. Both were unconsumed
  fixtures: the never-invoked `TestContext` stub now panics if called, and the
  awaited-leaf fixture reaches the `await` operation with a plain value
