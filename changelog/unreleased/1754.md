- `effects/node/virtual`: a path naming an inherited property (`toString`,
  `__proto__`) is absent in every operation, rather than reading as a
  `JsModule` or a directory — `access` and `readFile` claimed such a name
  existed, and `readFile` threw outside the effect's channel
