- `bnf`: add reusable JSON and DataJS grammars
- **BREAKING CHANGES:** move `repeat` from `bnf` to `types/array`, changing its result to a fixed-length tuple; remove the `Repeat` type from `bnf`; make `Option` branches readonly
