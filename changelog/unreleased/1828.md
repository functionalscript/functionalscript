- **BREAKING CHANGES:** `bnf/ll1` parsers now consume metadata-bearing code
  points. Add a metadata value to each input symbol; semantic transformer maps
  can now build values during LL(1) parsing without materializing a full AST.
