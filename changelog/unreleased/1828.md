- **BREAKING CHANGES:** BNF metadata now uses shared `Meta<M, T>` tuples.
  LL(1) inputs require these tuples, `CodePointMeta` is removed, LL(1) result
  types gain `M`, and BNF map and RTTI types replace `MI` and `MO` with one `M`.
- `bnf/ll1`: add semantic transformer maps that build values while parsing.
