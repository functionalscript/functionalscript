- `ci`: the Node 22 job runs `npm ci` and `node --test` through its generated
  Nix flake, so every canonical Node job now uses one; `setup-node` is left to
  the platform matrix.
- `ci`: every `nix develop` invocation passes `--no-write-lock-file`, so a CI
  run leaves no `flake.lock` behind. The pinned commit in `flake.nix` already
  determines every input.
