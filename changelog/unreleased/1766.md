- `rtti`: `validate` and `parse` answer a closed tuple's or struct's structure
  before reading any member — the length bound, then an illegal absence, then
  an undeclared member — so an `or` of two arities no longer walks shared
  operands once per arm. Acceptance is unchanged; those three errors now win,
  in that order, over a member's.
