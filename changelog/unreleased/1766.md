- `rtti`: `validate` and `parse` bound a closed tuple or struct by length before
  reading its members, so an `or` of two arities no longer walks shared operands
  once per arm. Acceptance is unchanged; a value that is both too long and wrong
  at a member now reports the container-level error rather than the member's.
