- `rtti`: `validate` and `parse` decide a closed tuple's or struct's member
  presence and settle any illegal absence, then bound it by length, before
  reading any member — so an `or` of two arities no longer walks shared
  operands once per arm. Acceptance is unchanged; a container-level or
  absent-member error now wins over an earlier member's.
