- `rtti`: `validate` and `parse` answer a closed tuple's or struct's structure
  before reading any member — presence recorded, length bound applied, then
  illegal absence settled — so an `or` of two arities no longer walks shared
  operands once per arm. Acceptance is unchanged; a container-level error now
  wins over an absent member's, and both over an earlier member's.
