- `rtti`: `validate` and `parse` decide a closed tuple's or struct's member
  presence, then bound it by length, before reading any member — so an `or` of
  two arities no longer walks shared operands once per arm. Acceptance is
  unchanged; a value that is both too long and wrong at a member now reports
  the container-level error rather than the member's.
