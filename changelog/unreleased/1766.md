- `rtti`: `validate` and `parse` answer a closed tuple's or struct's shape
  before reading any member, so an `or` of two arities no longer walks shared
  operands once per arm. A shape error now wins over a member's.
