- `emergent_testing`: the README now carries the two runners' shared design —
  why the run is sequential, why the browser runner is `.f.mjs` behind a thin
  host, and the thirteen-item pitfall catalog other modules cite by number —
  which was until now only in a finished `todo/` issue
