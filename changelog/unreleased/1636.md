- **BREAKING CHANGES:** `effects`: `IoEffect` is renamed `Effect`, with `E`
  defaulting to `NotImplemented`. The `Pure | Do` representation it was built on
  took the interim name `RawEffect`, which the entries below refer to; that name
  is deleted again before this release ships, and `Effect` is the representation
  now, so nothing in the published API is called `RawEffect`.
- **BREAKING CHANGES:** `effects`: `okStep` is removed. It was a step adapter
  whose only caller was the Io `step`, which now writes that branch itself.
