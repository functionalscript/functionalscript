- `website`: the generated page now runs the FunctionalScript proof suite in
  the browser and renders a pass/fail report.
- `emergent_testing`: new `browser.mjs` runs proof exports in a browser realm —
  `runBrowserProofs`, `startBrowserTests`, `startBrowserTestSources`, and
  `renderBrowserReport` — and publishes a serializable report.
