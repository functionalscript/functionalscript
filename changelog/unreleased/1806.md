- **BREAKING CHANGES:** `emergent_testing`: the browser runner moved from
  `emergent_testing/browser.mjs` to `emergent_testing/browser/module.mjs`,
  joining the pure half it was split from. Update the import path; no
  compatibility file is left behind
