- `emergent_testing`: new `browser/module.f.mjs` `reportOf` folds a browser
  run's results into its report — counts, status, and the host's own browser
  string and duration passed in rather than read
