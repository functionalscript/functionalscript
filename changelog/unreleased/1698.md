- `text/utf8`: a non-integer byte is reported invalid instead of decoded. A
  fractional continuation byte used to decode to the same code point its
  integer part spells, silently; `NaN` used to pass the range guard entirely
