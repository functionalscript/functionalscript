- **BREAKING CHANGES:** `emergent_testing`: `runModuleMap` answers the run's
  outcome — totals and leaf records — not an exit code; `exitCodeOf` derives
  that. The browser page shares `fjs t`'s traversal, through an interpreter
  that yields on a frame budget
