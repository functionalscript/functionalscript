- **BREAKING CHANGES:** `emergent_testing`: `runModuleMap` answers the run's
  outcome — totals and leaf records — instead of an exit code, which
  `exitCodeOf` now derives. The browser page runs the same traversal as
  `fjs t`, through a new browser effect interpreter
