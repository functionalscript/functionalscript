- **BREAKING CHANGES:** `emergent_testing`: `runModuleMap` answers the run's
  outcome — totals and leaf records — not an exit code, which `exitCodeOf`
  derives. The browser page runs the same traversal as `fjs t`, through a new
  browser interpreter
