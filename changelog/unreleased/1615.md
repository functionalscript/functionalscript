- `effects`: new `history`, `historyStep`, `foldStep` and `forEachStep` —
  the fallible chain keeps its earlier values, and a fold stops at the first
  error instead of running every item
- `ci`, `dev/update`, `nanvm/update`, `cas list`: a failed write is reported on
  stderr with exit code 1 rather than thrown
- `cas`: the staging upload composes through `fjs/effects`. A failed `now`
  or `randomInt` propagates instead of panicking, and a failed sweep can no
  longer fail the upload it runs before
