- **BREAKING CHANGES:** `path`: `normalize` and `concat` keep an absolute
  path's root and `..` cannot escape it, so `fjs compile` accepts an absolute
  module path. An absolute `concat` right-hand side replaces the left, and
  new `root` reports a root
