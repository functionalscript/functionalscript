- **BREAKING CHANGES:** `path`: an absolute path stays absolute — `parse`,
  `normalize` and `concat` keep its root, `..` cannot escape it, and an
  absolute right-hand side to `concat` replaces the left. A Windows `C:/` is a
  root too. New `root` reports a path's root. `fjs compile` now resolves
  imports from an absolute module path
