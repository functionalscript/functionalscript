- `fjs ci` now generates a `package-check` job: it downloads the packed tarball
  uploaded by the Node job, installs it as a dependency outside any checkout,
  and type-checks every declaration the package ships with the compiler version
  the package itself pins.
