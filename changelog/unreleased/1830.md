- `emergent_testing`: a module whose `proof` export cannot be enumerated is
  reported as one failed record named for the module, and the run continues.
  `fjs t` used to exit on the throw with no summary, losing every module that
  had not run yet
