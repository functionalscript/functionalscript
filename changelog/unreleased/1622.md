- **BREAKING CHANGES:** `types/rtti`: `validate` rejects an array longer than
  its tuple schema instead of accepting it, matching `Ts<T>` and the data form.
  A too-short tuple now reports at the root rather than at the first missing
  index. Structs are unchanged — extra keys are still accepted.
