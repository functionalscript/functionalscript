- **BREAKING CHANGES:** `types/rtti`: `validate` requires an array to match its
  tuple schema's length exactly, matching `Ts<T>` and the data form. An extra
  element is rejected, and so is a missing trailing `option`/`unknown` element,
  which used to pass. Structs are unchanged: extra keys are still accepted.
