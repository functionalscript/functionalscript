- **BREAKING CHANGES:** `fjs`: `run` reports a target it cannot use instead of
  panicking. A file that will not import, or one that imports without exporting
  a `main` function, is now a message on `stderr` and exit `1`.
