- **BREAKING CHANGES:** `website`: the browser source scanner moved from
  `website/browser-source.mjs` to `website/browser-source/module.f.mjs`. It is
  FunctionalScript now rather than plain `.mjs`; `exportsProof`, `specifiers`
  and `local` are unchanged. Update the import path; no compatibility file is
  left behind
