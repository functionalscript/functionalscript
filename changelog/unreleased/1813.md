- `ci`: a `flake.lock` is generated beside every `flake.nix` and committed, from
  `narHash` and `lastModified` in `config/module.f.mjs`. Nothing runs
  `nix flake lock`, so `fjs ci` still needs no Nix and still runs on Windows.
- `ci`: the `run` script passes `--quiet` once rather than three times. The
  extra two only hid `not writing modified lock file`, and took every other Nix
  warning with them; the lock removes the cause.
- `ci`: `/nix/*/flake.lock` is no longer in `.gitignore` — those files are
  generated output now, and the `node26` drift check compares them.
