- **BREAKING CHANGES:** `types/rtti/ts`: `Ts<T>` renders a tuple schema's
  trailing positions admitting `undefined` as optional, matching what `parse`
  and `validate` accept. A hand-written type pinned against one needs the `?`.
