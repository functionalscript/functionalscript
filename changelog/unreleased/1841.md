- `emergent_testing`: the README explains why the browser runner is `.f.mjs`
  behind a thin host — which values a definition needs from a host rather than
  whether it touches one, why reading a user value is effectful rather than
  impure, and the per-definition measure that says whether the boundary still
  holds
