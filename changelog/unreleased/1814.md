- `ci`: `dev.sh` at the repository root opens the shared Nix development shell
  — `nix develop ./nix`, the interactive counterpart to `nix/run`. It carries no
  flags and takes no arguments, so a first entry shows what it is fetching.
