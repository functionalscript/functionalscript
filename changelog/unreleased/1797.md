- `djs`: a compile error caused by an unterminated string or comment now
  reports the span of the offending source, `path:line:column-column`, rather
  than only where it started. Other errors still report a single position.
