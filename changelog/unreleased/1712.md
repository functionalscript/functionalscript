- `types/rtti`: `parse` and `validate` read a tuple schema by length, so a hole
  in one is a declared position whose schema is `undefined` — agreeing with the
  data form. Sparse schemas change acceptance; dense ones are unaffected.
