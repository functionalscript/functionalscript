- **BREAKING CHANGES:** `fsm`: DFA state keys are `sorted_set`'s canonical
  `toKey` rather than JSON text, so `run` returns different strings for the
  same automaton. `fjs/fsm` no longer depends on `fjs/media/json`
- `types/sorted_set`: new `toKey` builds a canonical, collision-free key for a
  set of strings
