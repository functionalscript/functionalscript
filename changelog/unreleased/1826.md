- Types: export `AbstractRequiredMap` to describe required-map APIs with an unrestricted string key set.
- Types: add `StateFold` for folds with explicit initialization, state updates, and finalization.
