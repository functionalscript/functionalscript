- `ci`: the `deno` and `bun` jobs run through generated Nix flakes, so every
  canonical job now uses one and no canonical job installs a runtime with a
  setup action. Both pins move to what the pinned Nixpkgs snapshot provides:
  Deno 2.8.3 and Bun 1.3.13.
