- `ci`: the `deno` job runs through a generated Nix flake, on the Deno the
  pinned Nixpkgs snapshot provides (2.8.3) rather than the one `setup-deno`
  installed.
- `ci`: the `deno` and `bun` jobs no longer install or run a published
  `functionalscript`. They ran the CLI of a shipped release, not of the commit
  under review.
