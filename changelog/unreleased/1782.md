- `ci`: the `deno` job runs through a generated Nix flake, on the Deno the
  pinned Nixpkgs snapshot provides (2.8.3) rather than the one `setup-deno`
  installed.
- `ci`: the `bun` job no longer installs or runs a published `functionalscript`.
  It ran the CLI of a shipped release, not of the commit under review.
