- **BREAKING CHANGES:** `fjs/ci/nix`: `nixDevelopAll` is removed — a CI step
  runs one command, so a job's sequence is one `nixDevelop` step per command
  rather than one `bash -c` bundle.
- `ci`: the Node 24 job runs `npm ci` and `node --test` through its generated
  Nix flake, one step each, instead of installing Node with `setup-node`.
