- `ci`: the Node 24 job runs through its generated Nix flake instead of
  `setup-node`: one `nix develop` invocation carrying a version check, `npm ci`
  and `node --test`. `fjs/ci/nix` exports the check as `nodeVersionCheck`.
