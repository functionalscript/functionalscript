- **BREAKING CHANGES:** `fjs/ci/nix`: generated flakes move from
  `nix/generated/<job>/` to `nix/<job>/`, so `generatedDirectory` is now `nix`.
- **BREAKING CHANGES:** `fjs/ci/nix`: `nixDevelopAll` and `nixVersionCheckStep`
  are removed, with the `nix-flakes` CI job. CI uses the generated flakes rather
  than checking them; what they declare is asserted by proofs.
- `ci`: the Node 24 job runs `npm ci` and `node --test` through its generated
  Nix flake, one step each, instead of installing Node with `setup-node`.
