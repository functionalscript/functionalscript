- `edag/amnesia`: new module — a tree-walking evaluator for `exp`. It memoizes
  nothing, so a shared node evaluates once per incoming edge and node identity
  is not preserved: it exists to prove node semantics, not to run
  FunctionalScript.
