- `effects`: new `effects/common`, for operations more than one host
  implements. `Sandbox` and `Catch` move there from `effects/node`, which
  re-exports both and spreads their handlers, so existing imports keep working
